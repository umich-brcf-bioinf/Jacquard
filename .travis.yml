#cgates/jebene: 7/9/2014

language: python
python:
    - "2.7"

install:
    - pip install -r requirements.txt
 
before_script:
    - pip install python-coveralls

script: 
    #- nosetests --with-coverage --cover-package=jacquard,variant_callers
    - nosetests

after_success:
    - coveralls

notifications:
    email:
        - cgates@umich.edu
        - jebene@umich.edu
        - kmeng@umich.edu
